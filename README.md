# CNN-Based-Image-Classification-using-PyTorch-CIFAR-10-
This project implements a Convolutional Neural Network (CNN) using PyTorch for multi-class image classification on the CIFAR-10 dataset. It includes data preprocessing, normalization, model training, and performance evaluation. The model learns to classify images into 10 different categories such as airplane, car, bird, cat, and more.
